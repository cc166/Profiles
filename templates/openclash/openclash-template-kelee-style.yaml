# OpenClash public sanitized template
# 说明：公开仓库仅保留脱敏模板占位，不放真实节点、真实 provider、真实订阅地址。
# 如需实际运行，请使用本地直读配置或 secret gist 私有备份。

# 占位字段示例：
proxy-providers:
  机场名称1:
    url: "机场1的订阅URL"
    type: http
    interval: 3600
    path: "./proxy_providers/机场名称1.yaml"

rules:
  - MATCH,DIRECT
