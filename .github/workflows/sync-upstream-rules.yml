name: Sync Upstream Rules

on:
  workflow_dispatch:
  schedule:
    - cron: '23 3 * * *'

jobs:
  sync:
    concurrency:
      group: sync-upstream-rules
      cancel-in-progress: true
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install browser-like curl fallback
        run: |
          python -m pip install --disable-pip-version-check curl_cffi
      - name: Sync upstream rule mirrors
        run: python upstream/scripts/sync_upstream_rules.py
      - name: Commit and Push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add upstream
          if git diff --cached --quiet; then
            echo "No upstream changes to commit"
            exit 0
          fi
          git commit -m "chore: sync upstream rules"
          git push
